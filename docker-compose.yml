version: "3"
label: "tree"
services:
  app:
    ports:
        -    "5505:5000"
    build:
      context: ./app
      dockerfile: Dockerfile_app
    deploy:
        resouces:
            limits:
                memory: 150M
  tester:
    build:
      context: ./test
      dockerfile: Dockerfile_tester
    depends_on:
      - app
    ports:
        -    "5033:22"
